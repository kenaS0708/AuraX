<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ========== РАЗРЕШЕНИЯ ========== -->

    <!-- BLE-сканирование (API 31+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <!-- Подключение к BLE-устройству (API 31+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Для API < 31 — классические BLE разрешения -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!-- Местоположение нужно для BLE-сканирования на API < 31 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Интернет для отправки фото на сервер -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Вибрация для тактильной обратной связи кнопки -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Объявляем что приложение использует BLE -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:allowBackup="true"
        android:label="BLE CAM"
        android:theme="@style/Theme.BleCam"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BleCam.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
