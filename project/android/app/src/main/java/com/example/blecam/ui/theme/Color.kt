package com.example.blecam.ui.theme

import androidx.compose.ui.graphics.Color

// ========== ЦВЕТОВАЯ ПАЛИТРА ==========
// Дизайн: тёмный, иммерсивный, минималистичный

/** Основной фон — почти чёрный */
val BackgroundDark = Color(0xFF0A0A0A)

/** Поверхность карточек — чуть светлее фона */
val SurfaceDark = Color(0xFF141414)

/** Поверхность с небольшим подъёмом */
val SurfaceElevated = Color(0xFF1E1E1E)

/** Основной акцентный цвет — холодный электрический синий */
val AccentBlue = Color(0xFF00D4FF)

/** Приглушённый акцент (для второстепенных элементов) */
val AccentBlueDim = Color(0x4400D4FF)

/** Свечение вокруг акцентных элементов */
val AccentGlow = Color(0x2200D4FF)

/** Зелёный для статуса BLE "подключено" */
val StatusConnected = Color(0xFF00FF88)

/** Серый для статуса BLE "отключено" */
val StatusDisconnected = Color(0xFF444444)

/** Основной текст */
val TextPrimary = Color(0xFFFFFFFF)

/** Второстепенный текст */
val TextSecondary = Color(0xFF888888)

/** Текст-плейсхолдер */
val TextHint = Color(0xFF444444)
